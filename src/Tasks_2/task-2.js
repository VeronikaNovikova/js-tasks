let points = [[2, 1], [2, 3], [5, 3], [4, 1]];

function checkRect(points) {
    console.log(points)
    debugger
    for (let i = 0; i < points.length; i++) {
        console.log(points[i])

        // for (let j = 0; j < points.length; j++) {
        //     console.log(points[j])
        // }
    }
}

// (x1 == x2 && x3 == x4 && y1 == y2 && y3 == y4)
// (x1 == x2 && x3 == x4 && y1 == y2 && y3 == y4)

checkRect(points)